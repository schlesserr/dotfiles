# set -g @plugin 'catppuccin/tmux'
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

#set -g @plugin 'tmux-plugins/tmux-resurrect'
##Enable mouse
set -g mouse on

# remap prefix
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# Start windows and panes at 1, not 0
set -g base-index 1
set -g pane-base-index 1
set -g renumber-windows on

# use 256 color terminal
set -g default-terminal "tmux-256color"
set-option -sa terminal-overrides ",alacritty:RGB"
# set -g default-terminal "alacritty"

# New window: c
unbind c
bind c new-window -c "#{pane_current_path}"

# Notifying if other windows have activity
# setw -g monitor-activity on
# set -g visual-activity on


# Reload Config
unbind r
bind r source-file ~/.tmux.conf \; display "Reloaded tmux config!"


# Vim Switch Panes
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

run '~/.tmux/plugins/tpm/tpm'

# --> Catppuccin (Mocha)
#bg_1 = "##4b4b58"
#bg_2 = "#353543"
thm_bg="#1e1e2e"
thm_fg="#cdd6f4"
thm_cyan="#89dceb"
thm_black="#181825"
thm_gray="#313244"
thm_magenta="#cba6f7"
thm_pink="#f5c2e7"
thm_red="#f38ba8"
thm_green="#a6e3a1"
thm_yellow="#f9e2af"
thm_blue="#89b4fa"
thm_orange="#fab387"
thm_black4="#585b70"

set-option -g status-justify left
set-option -g status-left "#[bg=#a6e3a1] #[bg=#4b4b58] #[bg=#353543] #[bg=#1e1e2e]#[fg=#fab387] #h #[bg=#353543] #[bg=#4b4b58] "
set-option -g status-left-length 16
set-option -g status-bg "#28283e"
# set-option -g status-right "#{?pane_synchronized, #[fg=#f38ba8]*SYNC*#[default],}#[bg=#28283e] #[bg=#1e1e2e]#[fg=#fab387] #(date '+%a %b %d %H:%M') #[bg=#28283e] #[bg=#32324d] #[bg=#a6e3a1] "
set-option -g status-right "#{?pane_synchronized, #[fg=#f38ba8]*SYNC*#[default],}#[bg=#28283e] #[bg=#1e1e2e]#[fg=#fab387] #(date '+%a %b %d %H:%M') #[bg=#353543] #[bg=#4b4b58] #[bg=#a6e3a1] "
set-option -g status-interval 60

set-option -g pane-border-status bottom
set-option -g pane-border-format "#{pane_index} #{pane_current_command}"
set-option -g pane-active-border-style fg="#b3b3b3"
set-option -g pane-border-style fg="#8a8a8a"

set-window-option -g window-status-format         "#[bg=#1e1e2e]#[fg=#f5c2e7] #I #[bg=#28283e]#[fg=#89b4fa] #[bg=#32324d]#W#[bg=#28283e]#[fg=#f38ba8]#F#[bg=#1e1e2e] "
set-window-option -g window-status-current-format "#[bg=#1e1e2e]#[fg=#a6e3a1] #I #[bg=#28283e]#[fg=#89b4fa] #[bg=#32324d]#W#[bg=#28283e]#[fg=#a6e3a1]#F#[bg=#1e1e2e] "
